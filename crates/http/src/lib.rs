//! Placeholder Axum server facade. Implementation arrives in later phases.

/// Start the HTTP server (stub).
pub fn start_server() {
    tracing::info!(target: "atlas-http", "server bootstrap pending implementation");
}
